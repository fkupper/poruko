apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: poruko

resources:
  - namespace.yaml
  - configmap.yaml
  - api.yaml
  - workers.yaml
  - web.yaml
  - ingress.yaml
  # Create secret separately — see README.md / secret.example.yaml
  # - secret.example.yaml

images:
  - name: ghcr.io/fkupper/poruko
    newTag: latest
